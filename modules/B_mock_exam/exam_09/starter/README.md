# Linux Debugging Starter

- Review `broken_service.sh` and `logs/app.log` to identify the restart loop.
- Produce `fixed_service.sh` with safe defaults and pre-flight checks.
- Add `sample.service` systemd unit and describe how you validated locally (e.g., run script in a loop for 30s and tail the log).
