# Bash Text Stats 01 – Simple Analyzer

Write a Bash script `text_stats.sh` that:

1. Accepts a file path as first argument.
2. Prints:
   - line count
   - word count
   - character count

Format:

```text
lines=<N>
words=<N>
chars=<N>
```

Tip: use standard tools like `wc`.
